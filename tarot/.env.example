# Supabase
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Session (also signs OAuth CSRF state)
SESSION_SECRET=

# AI: mock | deepseek  (legacy alias: gateway → deepseek)
AI_PROVIDER=mock
# Same OpenAI-compatible DeepSeek wiring as stock-dashboard:
# DEEPSEEK_API_KEY=
# DEEPSEEK_API_URL=https://mediocre-new-api.midway.run/v1/chat/completions
# DEEPSEEK_MODEL=deepseek-v4-flash

# OAuth (optional — if unset, password login still works; buttons hidden)
# OAUTH_BASE_URL=https://candle-taro.vercel.app
# GITHUB_OAUTH_CLIENT_ID=
# GITHUB_OAUTH_CLIENT_SECRET=
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# Redirect URI for both providers:
#   {OAUTH_BASE_URL}/api/auth/oauth/callback
#   (also add http://localhost:3000/api/auth/oauth/callback for local dev)
