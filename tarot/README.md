# Candle Taro (`tarot/`)

烛光占卜 Web 应用 — `le-web` monorepo 子项目。

产品决策见 `PLAN.md` / `DEV.md` / `COPY.md`。

## Dev

```bash
cd tarot
npm install
npm run dev
```

Vercel：独立项目，Root Directory = `tarot`。

## Status

P0 scaffold: Next.js + Tailwind + shadcn，深色烛光主题，首页 / 新占卜 / 阅读空壳 / 历史 / 设置。
