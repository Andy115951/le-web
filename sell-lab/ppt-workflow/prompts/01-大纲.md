# 提示：大纲

根据 `scenes/<slug>/spec.md` 写 `packs/<slug>/outline.md`。

只整理结构，不要生成 pptx。

大纲里必须有：

1. 套装名称、给谁交差、闲鱼搜索词
2. 配色（从 spec 抄，不要另起一套）
3. 逐页：页名、这一页买家要填什么、版式（封面 / 双栏 / 卡片 / 表 / 图）
4. 哪些字是占位、占位格式

约束：

- 页数不得超出 spec
- 不要增加「团队风采」「行业趋势」这类无关页
- 每一页都要能回答：周五晚上打开，这一页是改字还是删掉？删掉的页不要留
