# Stock / NDX Event Radar 产品路线图

本文档记录 Stock 项目的完整产品目标、功能范围、数据体系、Agent 工作流、数据库演进、研发阶段和验收标准。它是“要做什么”的长期依据；本地环境、部署和排错见 [DEVELOPMENT.md](DEVELOPMENT.md)，当前代码说明见 [README.md](README.md)，现有 Supabase SQL 见 [SUPABASE_SETUP.md](SUPABASE_SETUP.md)。

## 1. 最终定位

项目长期名称暂定为 `NDX Event Radar`，中文名为“纳斯达克 100 动态日历 / 纳指市场记忆系统”。

> 一个由 Agent 驱动的纳斯达克 100 事件日历、市场记忆和情景概率分析系统。

系统主要回答：

1. 今天市场发生了什么，哪些事件可能与涨跌有关？
2. 历史上哪些环境和事件组合与今天相似，后来如何演化？
3. 当前更接近哪些未来情景，各情景的历史概率、收益和风险是什么？

输出必须使用“归因假设”“关联线索”“情景概率”等克制表达，不把相关性包装成确定因果，不承诺明日涨跌。

## 2. 项目边界

### 要做

- 以 `QQQ` / Nasdaq-100 为核心基准
- 跟踪 Nasdaq-100 成分股、权重和历史变更
- 将价格、宏观、公司公告、财报和新闻按交易时间对齐
- 自动生成每日事件卡、市场复盘和来源引用
- 建立可检索的历史事件库和相似交易日库
- 输出未来 1/3/5/20 个交易日的情景统计
- 持续记录、回测和校准历史判断
- 保留个人自选、持仓上下文、决策纪律和复盘日志
- 支持 Web/PWA 和多设备查看

### 暂不做

- 自动下单和券商账户控制
- 高频交易、盘口预测或毫秒级行情
- “明天必涨/必跌”式结论
- 没有回测依据的仓位建议
- 无来源的 AI 新闻或历史原因倒推
- 第一阶段大规模社交媒体舆情抓取
- 第一阶段桌面客户端和本地大模型

项目只用于研究、信息整理和学习，不构成投资建议。任何预测或情景输出都必须同时展示样本量、置信度、风险和数据时点。

## 3. 当前基础

当前 `stock-dashboard` 已具备：

- 原生 Web 看板
- `QQQ`、`MAGS`、美国七巨头和自选股行情
- A 股 / 美股详情接口
- 浏览器 `localStorage` 状态
- Supabase Auth 和 `watchlist_states` 云同步
- 持仓成本、股数、持仓类型、目标价和回撤纪律
- 相对 `QQQ` 强弱与行动队列
- 当日公开资讯和涨跌关联线索
- 最近事件快照及 `market_event_history` 长期归档入口
- Vercel Functions 和收盘后 Cron 骨架
- Supabase CLI 本地配置
- 新版 `SUPABASE_SECRET_KEY` 服务端访问方式
- 不依赖个人自选的 Nasdaq 核心行情/新闻雷达
- 近 36 小时新闻聚合、去重和标题相关性过滤
- 未登录可用的本地 14 天日度脉络
- 公共 `nasdaq_market_event_history` 与无登录历史 API
- 收盘 Cron 在无用户数据时仍能归档核心市场事件

当前首页已经切换为“公共 Nasdaq 雷达优先，个人持仓辅助”，但长期数据仍需迁移到独立的市场日、价格、事件、来源、日报、预测和评估表，不能依赖 `watchlist_states` JSON。

## 4. 目标页面

### 4.1 首页 / 今日市场

- QQQ / NDX 当日涨跌、成交量和波动状态
- Nasdaq-100 上涨/下跌家数
- 主要贡献股和主要拖累股
- 行业/主题表现
- 美债收益率、美元和重要宏观变量
- 当天高影响事件
- AI 复盘摘要和证据来源
- 当前情景与历史相似样本摘要

### 4.2 动态日历

每个交易日展示 QQQ/NDX 涨跌、波动等级、事件数量、最高影响等级、宏观/财报/公告/指数调整标记、情景判断及后续验证结果。

支持月/周/列表视图，按事件类型、股票、行业和置信度筛选，明确区分非交易日与交易日，以美东交易日为主并标注北京时间。

### 4.3 单日详情

- QQQ / NDX / 主要指数表现
- 主要贡献股、拖累股和权重影响
- 宏观与公司事件时间线
- 原始来源、发布时间和市场可知时间
- 归因假设、支持证据和反例
- 历史相似日及后续 1/3/5/20 日表现
- 当时生成的情景判断
- 事后实际结果和评估

### 4.4 历史相似日

- 相似度总分
- 价格、宏观、事件、板块、波动分项相似度
- 相似依据与不相似点
- 后续收益分布、胜率、回撤和波动率

### 4.5 研究报告

- 每日收盘复盘
- 每周市场记忆摘要
- 财报季专题
- CPI / 非农 / FOMC 专题
- 单只成分股事件回放
- 某类事件历史统计报告

所有报告必须能回到来源和结构化数据，不能只保存不可解释的 AI 文本。

### 4.6 评估看板

- 历史情景数量
- 不同窗口的方向准确率
- 概率校准曲线
- 收益和最大回撤分布
- 不同市场状态和事件类型下的表现
- 模型/规则/提示词版本对比
- 失败案例与错误原因标签

## 5. 数据体系

### 5.1 价格数据

第一阶段保存 QQQ 和主要 Nasdaq-100 成分股日线 OHLCV、涨跌幅、振幅、成交量、跳空、回撤、趋势、波动率及未来 1/3/5/20 日收益标签。

后续增加 NDX 官方指数、全部成分股、调整后价格、拆股分红、行业主题篮子、个股对指数的日度贡献和盘前/盘中/盘后时段。

价格记录必须保存供应商、拉取时间、市场日期和调整方式。

### 5.2 Nasdaq-100 成分与权重

必须保存历史快照，不能用今天的成分回测过去：

- 生效日期
- 成分股和公司实体
- 权重
- 加入/移除原因
- 来源和发布时间
- 再平衡及特别再平衡事件

### 5.3 宏观数据

第一批：CPI、Core CPI、PPI、非农、失业率、FOMC、Fed 会议纪要、美国 2Y/10Y 收益率、利差和美元代理指标。

后续：零售销售、ISM、GDP、PCE、初请失业金、Fed 官员讲话和市场隐含利率概率。

每个宏观事件至少保存预期值、公布值、前值、修正值、发布时间、时区和来源。

### 5.4 公司官方事件

- SEC 10-K / 10-Q / 8-K
- Earnings Release
- 业绩指引变化
- 回购、并购、高管变动
- 重大诉讼和监管调查
- 产品发布和投资者日

公司事件关联 ticker、CIK、公司实体、事件发生时间和市场可知时间。

### 5.5 财报数据

- 计划日期、实际发布时间和盘前/盘后
- EPS / Revenue 预期、实际和 surprise
- 指引变化
- 电话会摘要和管理层关注点
- 财报后 1/3/5/20 日反应

### 5.6 市场结构事件

- Nasdaq-100 成分和权重调整
- 指数方法论变化
- 季度/年度及特别再平衡
- 大型期权到期
- ETF 资金流和重大被动资金再配置

### 5.7 新闻和舆情

公司、行业、监管、分析师和主题新闻需要去重、来源归并、保存原始 URL、首次发布时间和更新时间，并区分原始来源与转载。不能保存超出授权范围的全文。社交媒体仅作为后期弱信号。

## 6. 数据来源策略

优先级：官方 API/公告 → 交易所/指数公司 → 公司投资者关系 → 可靠数据供应商 → 新闻聚合 → 社交媒体。

来源方向：

- SEC EDGAR：filings 和 XBRL
- FRED：宏观时间序列
- 美国财政部或 FRED：国债收益率
- Nasdaq 官方：成分、方法论和调整公告
- 公司 Investor Relations：财报和公告
- 行情供应商：QQQ / NDX / 成分股 OHLCV

每个数据源接入前记录使用条款、频率限制、API Key、时间戳精度、历史覆盖、复权方式、修订机制和失败降级方案。

## 7. 统一事件模型

```json
{
  "event_id": "uuid",
  "event_time": "2026-08-07T20:05:00Z",
  "available_at": "2026-08-07T20:05:12Z",
  "market_date": "2026-08-07",
  "event_type": "earnings_guidance",
  "title": "公司更新业绩指引",
  "summary": "结构化中文摘要",
  "sentiment": "negative",
  "impact_scope": "sector",
  "impact_level": "high",
  "confidence": 0.78,
  "tickers": ["NVDA"],
  "themes": ["semiconductor", "ai_compute"],
  "source_ids": ["source-uuid"],
  "extractor_version": "event-labeler-v1",
  "created_at": "2026-08-07T20:06:00Z"
}
```

时间字段必须分开：`event_time` 是实际发生/发布时间，`available_at` 是系统或市场可获得时间，`captured_at` 是采集时间，`market_date` 是归属交易日。

## 8. 数据库目标模型

### 当前表

- `watchlist_states`：用户自选、偏好、持仓和短期事件 JSON
- `market_event_history`：用户/交易日/股票归档快照

### 市场基础表

- `instruments`
- `market_days`
- `price_bars_daily`
- `ndx_constituent_snapshots`
- `macro_series`
- `macro_observations`

### 事件与来源表

- `sources`
- `events`
- `event_sources`
- `event_entities`
- `earnings_events`
- `filings`

### 研究与评估表

- `daily_market_reports`
- `daily_features`
- `similar_day_matches`
- `scenario_forecasts`
- `forecast_factors`
- `forecast_evaluations`
- `model_runs`
- `decision_logs`

### 数据库约束

- 来源 URL 和内容指纹去重
- 市场日期通过统一交易日表校验
- 预测保存 `generated_at`，禁止事后覆盖原始版本
- 更新保留模型/规则版本
- 关键写入幂等
- 用户表启用 RLS
- 公共市场数据和用户私有数据分区

## 9. Agent 工作流

### Daily Collector Agent

拉取价格、宏观、filings、财报和新闻，记录数据源与时间，负责去重、重试和降级，不负责生成最终市场原因。

### Event Labeler Agent

把原始文档转为结构化事件，识别 ticker/公司/主题/行业，标注类型、范围、方向和置信度，低置信度进入复核。

### Market Attribution Agent

对齐价格和事件，生成多个可能解释，区分市场、公司、混合和证据不足，同时给出支持证据与反例，只输出归因假设。

### Historical Similarity Agent

计算价格、宏观、事件和板块状态相似度，排除未来数据，输出分项依据和后续收益/回撤/波动分布。

### Scenario Forecast Agent

读取统计模型概率和历史样本，构建情景、触发条件、失效条件和置信度，不越权生成确定性建议。

### Reporter Agent

生成结构稳定的中文日报/周报，引用事件和来源，突出新信息、市场分歧和风险。

### Backtest / Evaluator Agent

预测到期后自动评分，检查方向、收益、回撤和概率校准，标记失败案例和数据泄漏，不自动修改生产模型。

## 10. 预测与统计原则

数字由统计模型负责：Logistic Regression 做可解释基线，Gradient Boosting/LightGBM/XGBoost 做候选，简单历史条件统计做对照，波动率模型负责风险。第一版不急于深度学习。

LLM 负责事件抽取、filing/财报摘要、归因假设、相似案例解释和报告生成；不负责凭空产生概率、替代回测、修改原始价格或在缺少来源时补写原因。

预测输出至少包含窗口、情景概率、模型置信度、样本数、收益和回撤分布、正负因素、失效条件以及模型/数据版本。

## 11. 历史相似日

相似度由价格状态、波动状态、市场广度、宏观状态、事件组合和板块状态组成。

要求：

- 显示分项相似度，不只给单一向量分数
- 明确样本不足
- 连续日期避免重复占满结果
- 未来收益严格从匹配日之后开始
- 标准化参数只使用当时以前的数据拟合

## 12. 回测与评估

目标包括未来 1/3/5/20 日方向、收益、最大回撤和波动率。

指标包括 Accuracy、Balanced Accuracy、Precision/Recall、Brier Score、Log Loss、概率校准、平均/中位收益、最大回撤、概率分桶实际胜率，并与永远上涨、简单动量等基线比较。

必须使用 walk-forward：按时间滚动训练验证，只使用当时可知信息，保存宏观首次公布值，使用历史真实成分，测试集不反向参与规则调优。

## 13. 技术架构方向

当前：原生 Web + Vercel Functions + Supabase + Vercel Cron + Node.js。

中期：Web/PWA 为主，结构化数据在 Supabase，Python Worker 负责回填、特征和回测；Vercel 承载前端和轻量 API，重任务迁到 Worker 服务，原始文档按授权需求使用 Storage。

只有出现本地模型、离线研究、大量本地文件、本地数据库或交易终端联动需求后才考虑 Tauri 桌面端。

## 14. 环境变量规划

当前服务端必需：

```text
SUPABASE_URL
SUPABASE_SECRET_KEY
CRON_SECRET
```

浏览器使用 Supabase Project URL 和 Publishable Key。后续按需增加 `OPENAI_API_KEY`、`FRED_API_KEY`、`MARKET_DATA_API_KEY`、`NEWS_API_KEY`、`SENTRY_DSN`。真实值永远不进入 Git。

## 15. 可观测性与运维

每个任务保存 run_id、类型、开始/结束时间、数据日期、输入数、成功/失败数、重试数、数据源状态、代码版本和错误摘要。

需要建设 Cron 日志、失败告警、数据缺失/接口变化告警、单日补抓、日期范围回填、幂等重跑和任务看板。

## 16. 安全与隐私

- Secret Key 仅服务端使用
- Publishable Key 可在浏览器使用，但用户表必须启用 RLS
- Cron 使用独立 Secret
- Access Token 不进入聊天、文档、日志和仓库
- 错误日志不打印密钥、完整上游响应和隐私数据
- 持仓、成本和决策日志属于私有用户数据
- 公共数据和用户数据分表分权
- 泄露时立即撤销、轮换并检查访问日志

## 17. 关键数据风险

- 幸存者偏差：回测必须使用历史成分和权重
- 未来函数：收盘后信息不能解释或预测当天盘中
- 时间戳错误：发布、可知、采集和反应时间分开
- LLM 过度归因：允许 `unclear`，展示反例和证据不足
- 宏观修订：保留首次公布值和修订版本
- 公司行为：处理拆股、分红、代码变化和并购
- 样本过少：显示样本量，防止过拟合
- 数据授权：确认许可和再分发限制

## 18. 分阶段路线图

### Phase 0：现有看板云端闭环

- [x] 当日市场线索
- [x] `market_event_history` 代码入口
- [x] Vercel Cron 配置文件
- [x] 新版 Secret Key 请求方式
- [x] Supabase CLI 安装、登录、init 和 link
- [x] 创建正式 migration
- [ ] `supabase db push`
- [x] 远程 Schema 已通过 Management API 应用并验证
- [x] Vercel link
- [x] Vercel Production 环境变量
- [x] 生产部署
- [x] 验证 Cron 鉴权和任务执行
- [ ] 验证 Cron 写入
- [x] 运行日志、失败诊断和手动重跑方式
- [x] 公共 Nasdaq 核心雷达不依赖 `watchlist_states`
- [x] 动态新闻去重与结构化相关性过滤
- [x] 本地 14 天 Nasdaq 日度脉络
- [x] 公共 Nasdaq 历史表和 30/90/180 天读取 API
- [x] 公共收盘归档不依赖 `watchlist_states`

验收已完成：在 `sourceUsers = 0` 时，收盘采集仍成功写入 14 条公共市场事件；个人历史 Cron 继续兼容。

### Phase 1：QQQ 市场记忆 MVP

- [x] 建立 `market_days`、`instruments`、`price_bars_daily`
- [x] 回填至少 5 年 QQQ 日线
- [x] 保存未来 1/3/5/20 日收益、20 日最大回撤和实现波动率标签
- [x] 建立统一 `sources`、`events`、`event_sources` 和 `event_entities`
- [x] 建立首个官方 NDX 成分/权重历史快照，并让核心雷达动态读取高权重标的
- [x] 做月历和单日详情页
- [x] 每日卡片展示 QQQ 波动状态、核心成分线索和统一事件
- [x] 支持按月份和单个市场日期查询

验收已完成：任何已入库交易日都能从动态日历进入单日详情，查看价格、事件、可复核来源和当日有效的 NDX 成分快照；未来收益标签仅在事后研究区展示。

### Phase 2：官方事件骨架

- [ ] 接入 SEC EDGAR、FRED 和财报日历
- [x] 实现 SEC EDGAR filings collector、官方来源关系和接受时间字段
- [x] 实现 FRED 宏观观测 collector、稳定去重和保守可知时间处理
- [ ] 维护 Nasdaq-100 当前成分
- [x] 建立确定性事件分类、来源去重和追加式人工复核队列
- [x] 区分发布时间、可知时间和采集时间；未知精确发布时间不得伪造

当前状态：SEC 和 FRED collector 均已完成代码、稳定去重与离线验证；但 Vercel 目前既没有合规 `SEC_USER_AGENT`，也没有 `FRED_API_KEY`，因此生产任务尚未请求两者。FRED 只记录实际采集时已知的观测，不把日期字段伪装成精确发布时间。统一事件已新增确定性待复核分类和追加式人工审核记录，原始事件不会被审核覆盖。财报日历和公司 IR 仍未接入。验收：CPI/FOMC/核心 filings/主要财报自动形成结构化事件。

### Phase 3：历史成分与相似日

- [x] 保存历史成分与权重快照
- [x] 保存成分变更事件并建立新快照审核流程
- [x] 构建每日价格/事件特征（宏观特征待官方数据源接入后补充）
- [x] 实现相似度基线和结果页
- [x] 展示候选日的后续收益、回撤和波动
- [x] 聚合前 N 个候选的样本数、历史频率、收益分位和回撤分布
- [x] 增加防未来函数测试

当前验收：有至少 60 个历史样本的 QQQ 交易日均可重复计算相似日；候选日必须在目标日之前、其 20 日结果须在目标日当时已成熟，且相邻 20 个交易日内的重复阶段会被排除。NDX 候选快照必须先生成差异报告、人工审核并显式批准导入；导入后的加入、移除和权重变化会保存在独立历史事件表。宏观、行业和完整事件维度待后续官方数据源补齐。

### Phase 4：情景概率和回测

- [x] 冻结 QQQ 20 日结果的 16 折扩展式 walk-forward 切分与 20 交易日 embargo
- [x] 定义首个 20 日方向目标，并完成永远上涨、训练期条件动量对照基线
- [x] 建立训练期标准化与 L2 正则 Logistic Regression 候选、逐折系数和概率校准分桶
- [x] 建立受限浅层概率树候选，并在同一冻结切分、校准和固定晋升门槛下比较；当前未优于基线，保持 research-only
- [x] 建立非重叠 20 日概率门控 walk-forward 聚合回测；现有候选没有优于始终持有 QQQ，保持 research-only
- [x] 保存不可变研究输入版本，并在 20 日标签成熟后以追加式审计记录真实收益、回撤和波动；不将未晋升候选写成实时预测
- [x] 建立概率校准诊断工件；当前 Logistic 候选未优于条件动量，保持 research-only
- [x] 预先注册模型晋升门槛与确定性失败标签；当前 Logistic 复核为 `not_eligible`
- [x] 建立首版失败案例复核面板：展示固定门槛、指标差距和失败标签
- [x] 增加按冻结验证区间聚合的阶段级失败案例，保留无逐日预测的研究边界
- [x] 扩展失败案例页：按事后市场阶段筛选，并以独立冻结工件审阅每个验证折的收益、回撤和实现波动；不保留逐日预测或把阶段用作运行时输入
- [ ] LLM 根据数字生成情景文字

验收：概率可追溯到模型、数据版本和历史评估，不存在 LLM 凭空给数字。

### Phase 5：自动研究系统

- [ ] Collector、Labeler、Attribution、Similarity、Scenario、Reporter、Evaluator Agents
- [x] 固定泄漏安全的日度研究输入包契约，供后续 Reporter/Attribution 使用
- [x] 定义引用约束的研究摘要输出 Schema、拒绝规则和服务端审计表，并排除人工否决事件
- [x] 保存可重放的日度研究输入快照、稳定指纹和来源摘要
- [x] 将既有快照迁移为字段顺序无关的事实指纹，避免 JSONB 回读导致重复模型任务
- [x] 看板提供研究输入回放：摘要优先、按需加载完整快照，展示市场状态、证据、相似日与审核汇总
- [x] 实现默认关闭的受控模型摘要执行器：仅使用归档输入、每日请求上限、同输入同模型幂等、引用校验和追加式审计；支持官方 DeepSeek 或仅服务端 HTTPS 兼容网关，真实研究数据出站需 `DEEPSEEK_RESEARCH_DATA_APPROVED=true` 的独立确认
- [x] 每日确定性事实报告：从不可变研究输入快照生成 QQQ 收盘、涨跌与证据计数，并以快照+版本幂等归档；不调用模型、不输出交易结论
- [x] 每周确定性事实汇总：按纽约自然周动态聚合不可变日报，显示实际覆盖日、已观察 QQQ 区间变化和证据覆盖；覆盖不足三日明确标记 `limited`
- [x] 节假日感知冻结周报：对 2026–2028 年按 NYSE 官方全天休市日历计算应有日报，提前收盘仍算交易日；日历覆盖外回退为严格五个工作日校验，不把未知缺口猜成假日
- [x] 脱敏研究运行健康与失败告警：精确快照/结果计数、采集状态与确定性告警代码；具体运维日志继续受 Cron Secret 保护
- [x] 研究任务看板基础：为研究输入、日报、模型摘要和到期评估追加真实阶段账本；看板展示脱敏状态与有限计数，不提供公开重试
- [x] 归因审核看板：将既有只读审核队列接入页面，可按待核对/未审核/全部筛选并查看原始来源；人工结论仍只由 CLI 追加
- [x] 研究覆盖面板：汇总已归档快照、日报/周报、结果成熟度、审核积压和阶段账本；只描述可回放性与待办，不把覆盖数量误作质量认证或投资建议
- [x] 派生研究数据新鲜度：以最新 QQQ 价格日只读对比特征、未来标签和相似日的最近日期；只提示受控重建需求，不从缺少匹配行推断任务未运行
- [x] 研究集成准备度：在覆盖面板安全显示内置市场采集与 SEC / FRED / 模型摘要的就绪状态，支持多端环境核对且不公开密钥、联系人、环境变量值或失败原因
- [x] 模型出站预检状态：安全区分未配置、主动关闭、待批准研究数据出站与可运行，避免把安全门控误报为配置丢失
- [x] 当前历史情景摘要：首页只读复用最新已物化 QQQ 相似日，展示最多五个成熟候选的 5/20 日经验分布与回撤范围；不返回候选明细、不触发模型或数据出站，并明确不是预测概率或交易指令
- [x] 快照级研究流程回放：新快照精确关联同一次收盘运行的安全阶段摘要，再串联日报、已验证模型摘要状态与到期审计；历史未关联快照明确标记，不从日期或历史日志推测结果
- [x] 快照回放阶段计数：同次收盘运行的行情、归因、日报、周报和结果阶段仅回放固定白名单计数；不公开错误、标的、来源、内部 ID 或原始 JSON
- [ ] 扩展任务账本（部分完成）：已纳入真实 `market_collection`、`event_attribution`、`weekly_fact_report` 阶段和 `partial` 状态；已追加记录可重试阶段的真实尝试、排队耗时和运行耗时，失败不公开原始错误。后续仍需将 Labeler/Attribution 拆为独立运行，并提供受保护的诊断链接
- [ ] 某日完整 Agent 流程回放（含采集、模型输出与审计结果）

验收：任务可观测、可重跑、可审计，结论可回到来源和中间结果。

### Phase 6：个人研究与高级能力

- [ ] 一键决策日志和历史决策点
- [ ] 个性化纪律和个人复盘统计
- [ ] PWA 安装和通知
- [ ] 邮件/飞书/Slack 报告（按需）
- [ ] 本地模型或桌面端（按需）

验收：公共市场研究与个人记录分离，个人数据受 RLS 保护。

## 19. 下一开发批次

严格按顺序：

1. 配置真实 `SEC_USER_AGENT` 与 `FRED_API_KEY`，用手动命令验证生产写入；随后接入公司 IR 与财报日历，为日度特征和相似日补齐官方宏观/公司事件维度。
2. 在 Vercel Production 保持 `DEEPSEEK_API_KEY`、模型 ID、每日上限和显式开关的服务端配置；只有在明确批准把一份历史研究快照发送给第三方后，才用该快照做受控手动验证，随后观察首个收盘任务的审计记录，再把模型输出接入完整 Agent 流程回放。
3. 扩充官方宏观/公司事件特征后再重跑固定评估，并重新冻结事后阶段诊断工件；当前 Logistic 已按固定门槛判定为不能晋升。

公共市场表和 Cron 实际写入验证完成前不开始 AI 预测；价格、事件和时间戳质量稳定前不开始复杂机器学习。

## 20. 完成定义

### 数据功能

- Schema/migration 已提交并在本地与远程验证
- 重跑不重复
- 数据有来源、时间戳和版本
- 休市、缺失和修订有处理

### Agent 功能

- 输入输出 Schema 固定
- 工具调用可追踪
- 结论引用来源
- 失败有降级输出
- 有回放和评估方法

### 页面功能

- 桌面和移动端可用
- 加载、空数据和错误状态完整
- 不暴露 Secret
- 关键数字口径可解释
- 日历、详情和来源可导航

### 预测功能

- 目标和窗口明确
- 严格时间切分
- 有简单基线
- 完成概率校准
- 展示样本量和风险
- 自动保存事后结果

## 21. 待确认决策

- 继续在 `stock-dashboard` 演进，还是数据层稳定后拆出 `ndx-event-radar`
- QQQ 与 NDX 哪个作为第一主基准
- 历史价格主供应商及授权
- 新闻供应商及预算
- 是否需要付费 Nasdaq 权重数据
- 第一版宏观事件范围
- AI 模型供应商和每日成本上限
- 报告只供个人使用还是支持多用户
- 是否长期保留 A 股功能或与 NDX 分离

这些决策不阻挡 Phase 0，但应在 Phase 1 建表和 UI 重构前确认。
